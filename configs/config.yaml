# 本文件记录服务的默认运行参数。运行时优先读取环境变量，未设置时使用下方默认值。
server:
  port: "8080"

database:
  host: "mysql"
  port: "3306"
  user: "blog"
  password: "blog_password"
  name: "blog"

jwt:
  secret: "change-me-to-a-long-random-secret"
